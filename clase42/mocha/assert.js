const mocha = require('mocha')
const assert = require('assert')

describe('test group name', () => {
    it('esta funcion debería hacer esto', () => {
        const infinito = Infinity;
        assert.strictEqual(infinito, Infinity);
    })
})