html(lang="es")
    head
        title='Productos'
    body
        h1='Plataforma de ingreso de productos'
        div
            h2='Ingresar producto nuevo'
            form(action="/productos" method="post")
                input(placeholder="Titulo del producto" type="text" name="title" required)
                input(placeholder="Precio" type="number" name="price" required)
                input(placeholder="Ruta de la imagen" type="text" name="thumbnail")
                input(type="submit" value="Enviar formulario")
            if producto
                h2='Último producto agregado'
                h3=producto.title
                p=producto.price
                img(src=producto.thumbnail alt=producto.title)
        a(href='/productos') Ver lista de productos